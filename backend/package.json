{
  "name": "spacetour-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Space Tour reservation backend — Lambda functions for AWS API Gateway, DynamoDB, SQS and SES.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/util-dynamodb": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0",
    "uuid": "^9.0.1"
  }
}
